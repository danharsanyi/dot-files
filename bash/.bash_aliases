# Clears the terminal
alias c="clear"

# Fetches from the current directories remote repo
alias gp="git pull"

# Runs yarn start
alias ys="yarn start"

# Runs yarn
alias y="yarn"

# Runs yarn generate
alias yg="yarn generate"

# Runs yarn extracto
alias ye="yarn extract-i18n"

# Runs yarn typegen
alias yt="yarn typegen"

#Runs yarn lerna run build
alias yl="yarn lerna run build"
